{
  "name": "slides",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "marp --server --watch",
    "build": "marp",
    "lint": "prettier --check ./",
    "fix": "prettier --write ./"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "4.4.0",
    "prettier": "3.8.3"
  }
}
